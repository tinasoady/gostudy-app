import 'package:flutter/material.dart';

class PublishScreenWidget extends StatelessWidget {
  const PublishScreenWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Publier')),
      body: const Center(child: Text('Écran Publier — placeholder')),
    );
  }
}
